#!/usr/bin/env perl

#use strict;
#use warnings;
#
#if ( -d '.svn' ) {
#    my @info = qx/svn info/;
#    chomp @info;
#    my %info = map { split /:\s+/, $_, 2 } @info;
#    print "\e[01;30m$info{URL} -r$info{Revision}\e[0m ";
#}
