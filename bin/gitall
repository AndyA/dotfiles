#!/bin/bash

find . -maxdepth 2 -name .git -type d -print | while read gitd; do
  repd="$( dirname "$gitd" )"
  echo "[ $repd ] git $@"
  pushd "$repd" >/dev/null 2>&1
  git "$@"
  popd >/dev/null 2>&1
done

# vim:ts=2:sw=2:sts=2:et:ft=sh

