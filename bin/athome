#!/usr/bin/env bash

home='ripley'
remote='vpn.hexten.net'
dig $home txt | grep ^$home > /dev/null
athome=$?
for dom in $*; do
  if [ $athome == 0 ]; then
    echo $dom
  else
    echo ${dom/$home/$remote}
  fi
done
exit $athome
