#!/usr/bin/env perl

use v5.10;

use autodie;
use strict;
use warnings;

my ( $phrase, @idx ) = @ARGV;

my @chars = split //, " $phrase";
say join(', ', @chars[@idx]);

# vim:ts=2:sw=2:sts=2:et:ft=perl

