#!/usr/bin/env perl

use latest;

say chaselink($_) for @ARGV;

sub chaselink {
  my $target = my $obj = shift;
  while ( defined ( my $next = readlink $target ) ) {
    $target = $next;
  }
  return $target;
}

# vim:ts=2:sw=2:sts=2:et:ft=perl

