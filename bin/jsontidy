#!/usr/bin/env node

"use strict";

let text = [];

process.stdin
  .resume()
  .setEncoding("utf8")
  .on("data", chunk => text.push(chunk))
  .on("end", () => {
    process.stdout.write(JSON.stringify(JSON.parse(text.join()), null, 2));
    process.stdout.write("\n");
  });
