# config.pl

use strict;
use warnings;

sub find_perls {

}

set_config(
    dists => [
        'HTML::Tiny' => {
            svn =>
              'http://svn.hexten.net/andy/Perl/HTML/HTML-Tiny/trunk'
        }
    ],
);
