# pind and cdpin

# Pin the current directory.
function pind() {
  local key=${1:-=}
  hasle ~/.pind -p $key "`pwd`"
}

# CD to a pinned directory
function cdpin() {
  local key=${1:-=}
  cd "`hasle ~/.pind -g $key`"
}

# vim:ts=2:sw=2:sts=2:et:ft=sh
